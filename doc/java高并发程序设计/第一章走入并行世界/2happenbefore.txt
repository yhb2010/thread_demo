1、程序顺序原则：一个线程内保证语义的串行性
2、volatile：volatile变量的写，先发生与读，这保证了volatile变量的可见性
3、锁规则：解锁必然发生在随后的加锁前
4、传递性：A先于B，B先于C，则A必然先于C
5、线程的start方法先于它的每一个动作
6、线程的所有操作先于线程的终结(Thread.join())
7、线程的中断(Interrupt())先于被中断线程的代码
8、对象的构造函数执行、结束先于finalize()方法