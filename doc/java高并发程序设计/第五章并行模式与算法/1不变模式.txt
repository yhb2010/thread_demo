和只读属性的区别：
	只读属性的对象，对象本身不能被其它线程修改，但是对象的内部状态却可以自行修改。比如存活时间字段是只读的，但这是一个可变属性。

主要使用场景：
	1、当对象创建后，其内部状态和数据不再发生任何变化
	2、对象需要被共享，被多线程频繁访问

不变对象的生成：
	1、去除setter方法以及所有修改自身属性的方法
	2、将所有属性设为私有，并用final标记
	3、确保没有子类可以重载修改它的行为
	4、有一个可以创建完整对象的构造函数

java中的应用主要是基本数据类型和String类型，都是不变模式。