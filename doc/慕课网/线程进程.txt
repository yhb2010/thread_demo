进程：
	打开一个QQ或者eclipse等，这就是一个进程，代表程序的执行过程（强调动态性），持有资源（共享内存、共享文件）和线程。
线程：
	当打开eclipse后，你编写了一个jsp文件，eclipse会在后台给你进行语法校验，这就是一个线程。
举个例子：
	进程可以比喻成一个班级，班级里每个学生都是一个线程，他们可以共用班级里的桌椅、黑板等进行学习生活。
线程是系统中最小的执行单元，同一个进程中可以有多个线程，线程共享进程的资源。

线程交互：
	互斥、同步。